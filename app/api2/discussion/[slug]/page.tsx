import prisma from "@/lib/prisma";

export default async function DiscussionPostPage(
    discussion_id,
    discussion_title,
){
const { slug } = await params
const post = await getDiscussion(params)

}